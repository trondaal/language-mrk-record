'use babel';

import LanguageMrkRecordView from '../lib/language-mrk-record-view';

describe('LanguageMrkRecordView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
